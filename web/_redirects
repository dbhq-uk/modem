# The three routes (/demo, /originate, /receive) are NOT here, and that is
# deliberate. Cloudflare Pages would not honour a 200 rewrite for them:
# rewriting to /index.html canonicalised straight to /, and rewriting to
# /app.html went to /app - both times throwing away the path that page.js
# reads to decide which route it is, which silently broke sharing a
# /receive link while looking fine locally.
#
# They are real directories instead, built from the one index.html at
# deploy time - see the "Assemble the upload" step in
# .github/workflows/deploy.yml, and spike/serve.py's RewritingHandler for
# the local equivalent.

# Old links to the file itself.
/index.html   /   301

# links.html was absorbed into explained.html entirely on 9 Sep 2026 (see
# the design spec) - the standards/research/history content plus the
# nine phases, now with a URL of their own. The file itself is gone;
# anyone with the old URL still lands somewhere real.
/links   /explained   301

# /prior-art was renamed /research on 10 Sep 2026 (Dan). Same page, same
# content; "prior art" named the legal-ish concept rather than what the
# page is - standards, related tools and the history. Nothing was indexed
# under the old URL (GSC showed 0 of 6 pages indexed at the time), so this
# is mostly for anyone holding a link.
/prior-art   /research   301
